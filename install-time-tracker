#! /bin/sh
echo Setting up uv environment
uv tool install --python 3.13 .

echo installing SwiftBar Plugin
ln -f -s `which time_tracker` `defaults read com.ameba.SwiftBar PluginDirectory`/time_tracker.1m.py

echo installing launch agent
cat <<EOF > ~/Library/LaunchAgents/de.oefelein.time-tracker.plist
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>de.oefelein.time-tracker</string>
	<key>ProgramArguments</key>
	<array>
		<string>`which time_tracker_agent`</string>
	</array>
    <key>KeepAlive</key>
    <true/>
	<key>WorkingDirectory</key>
	<string>$PWD</string>
</dict>
</plist>
EOF

launchctl load ~/Library/LaunchAgents/de.oefelein.time-tracker.plist

echo done!